# Environment Configuration
ENVIRONMENT=development
DEBUG=true

# Application
APP_NAME="Worklog Automation"
APP_VERSION="0.1.0"

# Server
HOST=0.0.0.0
PORT=8000

# Security (Change these in production!)
SECRET_KEY="your-super-secret-key-change-this-in-production-at-least-32-chars-long"                                                                             
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# Database (SQLite for development, PostgreSQL for production)
DATABASE_URL="sqlite+aiosqlite:///./worklog.db"
# For PostgreSQL: DATABASE_URL="postgresql+asyncpg://user:password@localhost:5432/worklog"                                                                      

# Redis (for caching and background tasks)
REDIS_URL="redis://localhost:6379/0"
CELERY_BROKER_URL="redis://localhost:6379/1"
CELERY_RESULT_BACKEND="redis://localhost:6379/2"

# Work Schedule
WORK_START_TIME="08:00"
WORK_END_TIME="18:00"
DEFAULT_TIMEZONE="UTC"

# External Integrations (Optional)
TEAMS_ENABLED=false
TEAMS_WEBHOOK_URL=""

JIRA_ENABLED=false
JIRA_BASE_URL=""
JIRA_USERNAME=""
JIRA_API_TOKEN=""

# Git Integration
GIT_ENABLED=true
DEFAULT_GIT_REMOTE="origin"

# File Storage
UPLOAD_DIR="./uploads"
MAX_UPLOAD_SIZE=10485760
ALLOWED_EXTENSIONS=".txt,.pdf,.doc,.docx,.png,.jpg,.jpeg"

# Logging & Monitoring
LOG_LEVEL="INFO"
ENABLE_METRICS=true

# Sentry Error Tracking (Optional)
# Get your DSN from https://sentry.io/settings/{organization}/projects/{project}/keys/
SENTRY_DSN=""
SENTRY_SEND_DEFAULT_PII=true
SENTRY_ENABLE_LOGS=true
SENTRY_TRACES_SAMPLE_RATE=1.0
SENTRY_PROFILE_SESSION_SAMPLE_RATE=1.0
SENTRY_PROFILE_LIFECYCLE="trace"

# Rate Limiting
RATE_LIMIT_ENABLED=true
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW=60

# Development
RELOAD=true
